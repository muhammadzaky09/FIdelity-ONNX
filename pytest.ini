[pytest]
testpaths =
    tests/unit
    tests/integration
    tests/cuda
python_files = test_*.py
markers =
    cuda: requires CUDAExecutionProvider and CUDA-compatible custom ops
    custom_ops: validates ONNX Runtime custom op libraries
    slow: slower validation that may need model assets
